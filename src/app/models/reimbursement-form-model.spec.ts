import { ReimbursementFormModel } from './reimbursement-form-model';

describe('ReimbursementFormModel', () => {
  it('should create an instance', () => {
    expect(new ReimbursementFormModel()).toBeTruthy();
  });
});
