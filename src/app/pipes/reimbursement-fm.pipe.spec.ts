import { ReimbursementFMPipe } from './reimbursement-fm.pipe';

describe('ReimbursementFMPipe', () => {
  it('create an instance', () => {
    const pipe = new ReimbursementFMPipe();
    expect(pipe).toBeTruthy();
  });
});
